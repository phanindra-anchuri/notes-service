(ns notes-service.temp)(ns notes-service.core  (:require    ;[compojure.core :refer :all]    [compojure.api.sweet :refer :all]    [ring.adapter.jetty :refer :all])  (:gen-class));(defroutes main-routes;  (GET "/notes" [] "All notes");  (GET "/notes/{id}" "Individual note"));(def app;  (routes;    (context "/notes" [];             (GET "/" [];               "All notes");             (GET "/:id" [id];               (str "note " id)))));(defn -main [];  (run-jetty main-routes {:port 3000}));(defapi app;  (context "/notes" [];    (GET "/" [];      "all notes");    (GET "/:id" [id];      (str "note " id))))